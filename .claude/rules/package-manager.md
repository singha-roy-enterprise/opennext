# Package manager

- Use `pnpm` for all package management — never `npm` or `yarn`.
- Always run package scripts with `pnpm run <script_name>` (e.g. `pnpm run dev`, `pnpm run build`).
  Do NOT use the shorthand `pnpm <script_name>` (e.g. `pnpm build`).
